Get-Process electron,cloudflared -ErrorAction SilentlyContinue | Stop-Process -Force
npm.cmd start

git pull

git add .
git commit -m "обновление"
git push

# Перед сборкой — убить всё + VS Code
Get-Process LaklyCustom, electron, cloudflared -ErrorAction SilentlyContinue | Stop-Process -Force
Get-Process Code -ErrorAction SilentlyContinue | Stop-Process -Force
Start-Sleep 2

# Чистая пересборка
cd C:\Users\Administrator\Desktop\LaklyTech\Apps\LaklyCustom
Remove-Item -Recurse -Force .\release -ErrorAction SilentlyContinue
Remove-Item C:\temp\lakly-boot.log -Force -ErrorAction SilentlyContinue
npm.cmd run dist

# Снести старую установку
$uninst = Get-ChildItem "$env:LOCALAPPDATA\Programs\LaklyCustom" -Filter "Uninstall*.exe" -ErrorAction SilentlyContinue
if ($uninst) { Start-Process $uninst.FullName -ArgumentList "/S" -Wait }
Start-Sleep 3
Remove-Item -Recurse -Force "$env:LOCALAPPDATA\Programs\LaklyCustom" -ErrorAction SilentlyContinue

# Установить
cd .\release
Start-Process ".\LaklyCustom Setup 0.8.0.exe" -Wait

# Проверить лог
Start-Sleep 6
Get-Content C:\temp\lakly-boot.log | Select-String -Pattern "LOAD DONE|plugin load failed|pluginsCache"